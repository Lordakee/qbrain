# N4a/N4b HARD AUDIT — PASS
Multi base_url already (embedding+chat separate). Think single-round + save gates. Provider recipes = config plane (OpenAI-compatible). Multi-round deferred as optional enhancement (documented).
